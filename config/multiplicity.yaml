exp_type: multiplicity
exp_name: mult_debug

seed: 42

use_mlflow: false

data:
 data_dir: data/
 dataset: zplusjets
 length: 128
 split: [0.8, 0.1, 0.1]
 rescale_data: false
 max_num_particles: 152

dist: 
 type: GammaMixture
 n_components: 3

loss:
 type: cross_entropy
 smoothness: 1

train: true
training:
 iterations: 5000
 batchsize: 128
 lr: 1e-3
 validate_every_n_steps: 50000000
 force_xformers: false # 'false' runs on cpu and gpu, 'true' only runs on gpu but is much faster

evaluate: true
evaluation:
 batchsize: 128

plot: true
plotting:
 n_distributions: 128

defaults:
 - model: tr_multiplicity
 - default
 - _self_